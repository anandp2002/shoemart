import { FiStar } from 'react-icons/fi'; import { FaStar, FaStarHalfAlt } from 'react-icons/fa'; const Rating = ({ value, count, showCount = true, size = 'text-sm' }) => { const stars = []; for (let i = 1; i <= 5; i++) { if (i <= Math.floor(value)) { stars.push(<FaStar key={i} className="text-amber-500" />); } else if (i - 0.5 <= value) { stars.push(<FaStarHalfAlt key={i} className="text-amber-500" />); } else { stars.push(<FiStar key={i} className="text-neutral-400" />); } } return ( <div className={`flex items-center gap-1 ${size}`}> <div className="flex">{stars}</div> {showCount && count !== undefined && ( <span className="text-neutral-400 text-xs ml-1">({count})</span> )} </div> ); }; export default Rating; 