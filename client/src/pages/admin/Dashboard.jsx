import { useEffect } from 'react'; import { useDispatch, useSelector } from 'react-redux'; import { Link } from 'react-router-dom'; import { FiPackage, FiDollarSign, FiUsers, FiBox, FiTrendingUp, FiEye } from 'react-icons/fi'; import API from '../../api/axios'; import { useState } from 'react'; import Loader from '../../components/atoms/Loader'; import { formatPrice } from '../../utils/formatPrice'; const AdminDashboard = () => { const [stats, setStats] = useState(null); const [loading, setLoading] = useState(true); useEffect(() => { const fetchStats = async () => { try { const { data } = await API.get('/orders/stats'); setStats(data); } catch (err) { console.error('Failed to fetch stats:', err); } finally { setLoading(false); } }; fetchStats(); }, []); if (loading) return <Loader />; const statCards = [ { icon: <FiDollarSign />, label: 'Total Revenue', value: formatPrice(stats?.totalRevenue || 0), color: 'bg-green-50 text-green-600' }, { icon: <FiPackage />, label: 'Total Orders', value: stats?.totalOrders || 0, color: 'bg-blue-50 text-blue-600' }, { icon: <FiBox />, label: 'Products', value: '-', color: 'bg-purple-50 text-purple-600' }, { icon: <FiUsers />, label: 'Customers', value: '-', color: 'bg-orange-50 text-orange-600' }, ]; return ( <div className="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-8 "> <div className="flex items-center justify-between mb-8"> <div> <h1 className="font-sans text-2xl md:text-3xl font-bold">Admin Dashboard</h1> <p className="text-neutral-400 mt-1">Manage your store</p> </div> </div> {/* Stat Cards */} <div className="grid grid-cols-2 lg:grid-cols-4 gap-4 mb-8"> {statCards.map((stat, i) => ( <div key={i} className="bg-white rounded-2xl shadow-md overflow-hidden transition-all duration-300 hover:shadow-xl rounded-xl p-5"> <div className={`w-10 h-10 rounded-xl ${stat.color} flex items-center justify-center mb-3`}> {stat.icon} </div> <p className="text-2xl font-bold">{stat.value}</p> <p className="text-sm text-neutral-400">{stat.label}</p> </div> ))} </div> {/* Quick Links */} <div className="grid grid-cols-1 md:grid-cols-3 gap-4 mb-8"> <Link to="/admin/products" className="bg-white rounded-2xl shadow-md overflow-hidden transition-all duration-300 hover:shadow-xl rounded-xl p-5 hover:border-primary border-2 border-transparent transition-all"> <FiBox className="w-6 h-6 text-amber-500 mb-2" /> <h3 className="font-semibold">Manage Products</h3> <p className="text-sm text-neutral-400">Add, edit, or remove products</p> </Link> <Link to="/admin/orders" className="bg-white rounded-2xl shadow-md overflow-hidden transition-all duration-300 hover:shadow-xl rounded-xl p-5 hover:border-primary border-2 border-transparent transition-all"> <FiPackage className="w-6 h-6 text-amber-500 mb-2" /> <h3 className="font-semibold">Manage Orders</h3> <p className="text-sm text-neutral-400">View and update order statuses</p> </Link> <Link to="/admin/users" className="bg-white rounded-2xl shadow-md overflow-hidden transition-all duration-300 hover:shadow-xl rounded-xl p-5 hover:border-primary border-2 border-transparent transition-all"> <FiUsers className="w-6 h-6 text-amber-500 mb-2" /> <h3 className="font-semibold">Manage Users</h3> <p className="text-sm text-neutral-400">View and manage user accounts</p> </Link> </div> {/* Recent Orders */} <div className="bg-white rounded-2xl shadow-md overflow-hidden transition-all duration-300 hover:shadow-xl rounded-xl p-6"> <h3 className="font-sans font-bold text-lg mb-4">Recent Orders</h3> {stats?.recentOrders?.length > 0 ? ( <div className="overflow-x-auto"> <table className="w-full text-sm"> <thead> <tr className="text-left border-b border-neutral-200"> <th className="pb-3 font-semibold">Order ID</th> <th className="pb-3 font-semibold">Customer</th> <th className="pb-3 font-semibold">Amount</th> <th className="pb-3 font-semibold">Status</th> <th className="pb-3 font-semibold">Date</th> </tr> </thead> <tbody> {stats.recentOrders.map((order) => ( <tr key={order._id} className="border-b border-neutral-100"> <td className="py-3 font-mono text-xs"># {order._id.slice(-8).toUpperCase()}</td> <td className="py-3">{order.user?.name || 'N/A'}</td> <td className="py-3 font-medium">{formatPrice(order.totalPrice)}</td> <td className="py-3"> <span className={`inline-flex items-center px-2.5 py-0.5 rounded-full text-xs font-semibold ${order.orderStatus === 'Delivered' ? 'inline-flex items-center px-2.5 py-0.5 rounded-full text-xs font-semibold bg-emerald-100 text-emerald-700' : order.orderStatus === 'Cancelled' ? 'inline-flex items-center px-2.5 py-0.5 rounded-full text-xs font-semibold bg-red-100 text-red-700' : 'inline-flex items-center px-2.5 py-0.5 rounded-full text-xs font-semibold bg-amber-100 text-amber-700' }`}> {order.orderStatus} </span> </td> <td className="py-3 text-neutral-400"> {new Date(order.createdAt).toLocaleDateString('en-IN', { day: 'numeric', month: 'short' })} </td> </tr> ))} </tbody> </table> </div> ) : ( <p className="text-neutral-400 text-center py-8">No orders yet</p> )} </div> </div> ); }; export default AdminDashboard; 